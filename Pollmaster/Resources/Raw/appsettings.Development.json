{
  "_comment": "Loaded ONLY in Debug builds (see MauiProgram.ConfigureConfiguration). Lets the Windows MAUI client talk to a locally-run backend without touching the production base address.",
  "PollmasterApi": {
    "BaseAddress": "https://localhost:7100/",
    "TimeoutSeconds": 60
  }
}
